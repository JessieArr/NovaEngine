[package]
name = "nova_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.33"
rand = "0.10.0"
ron = "0.12.0"
serde = { version = "1.0.228", features = ["derive"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
